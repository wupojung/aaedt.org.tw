{
  "name": "my-web-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:sass": "sass src/assets/theme.scss style.css --no-source-map",
    "watch:sass": "sass src/assets/theme.scss style.css --watch --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.101.0",
    "vite": "^7.3.1",
    "vite-plugin-html-inject": "^1.1.2"
  }
}
